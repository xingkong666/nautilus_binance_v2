# 交易对配置
# 使用 NautilusTrader TestInstrumentProvider 名称映射
# 默认 live 启动会按这里的 market_cap_rank / 文件顺序，选择前 N 个非稳定币 base asset 交易对。

instruments:
  BTCUSDT:
    provider: btcusdt_perp_binance
    market_cap_rank: 1
  ETHUSDT:
    provider: ethusdt_perp_binance
    market_cap_rank: 2
