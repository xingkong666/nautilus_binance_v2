# Binance合约账户配置
account:
  name: binance_futures_main
  venue: BINANCE
  account_type: MARGIN
  oms_type: HEDGING
  base_currency: USDT

  # 资金配置
  starting_balance: 10000  # USDT
  max_leverage: 10

  # 应用程序编程接口从环境变量读取
  # BINANCE_API_KEY / BINANCE_API_SECRET
